/**
 * FairSight AI — PDF Export Utility
 * Generates PDF audit reports using jsPDF + html2canvas.
 */

// TODO: Day 7 — implement full PDF export
export async function exportAuditPDF(auditId: number): Promise<void> {
  console.log(`PDF export for audit ${auditId} — coming Day 7`);
  throw new Error("PDF export not yet implemented");
}

export async function exportGroupStatsCSV(auditId: number): Promise<void> {
  console.log(`CSV export for audit ${auditId} — coming Day 7`);
  throw new Error("CSV export not yet implemented");
}
